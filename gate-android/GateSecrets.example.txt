package com.rkiwi.gate;

/**
 * Local credentials, not committed to git.
 *
 * Copy from GateSecrets.example.java and fill in real values.
 * The repo must never contain live Firebase passwords or household credentials.
 */
final class GateSecrets {
    static final String FIREBASE_API_KEY = "YOUR_FIREBASE_WEB_API_KEY";
    static final String RTDB_URL = "https://YOUR_PROJECT-default-rtdb.YOUR_REGION.firebasedatabase.app";
    static final String EMAIL = "user@example.com";
    static final String PASSWORD = "change-me";

    private GateSecrets() {}
}
